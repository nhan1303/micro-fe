import { S as SvelteComponent, i as init, f as safe_not_equal, j as element, k as insert, n as noop, l as detach } from './Component-CkNjPqT0.js';

// generated during release, do not modify

const PUBLIC_VERSION = '4';

if (typeof window !== 'undefined')
	// @ts-ignore
	(window.__svelte || (window.__svelte = { v: new Set() })).v.add(PUBLIC_VERSION);

/* src/App.svelte generated by Svelte v4.2.12 */

function create_fragment(ctx) {
	let main;

	return {
		c() {
			main = element("main");
			main.innerHTML = `<p class="hello svelte-a25kvq">Hello from Svelte</p>`;
		},
		m(target, anchor) {
			insert(target, main, anchor);
		},
		p: noop,
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) {
				detach(main);
			}
		}
	};
}

class App extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, null, create_fragment, safe_not_equal, {});
	}
}

const entry = {
  mount(el = "#app") {
    const target = typeof el === "string" ? document.querySelector(el) : el;
    const app = new App({ target });
    console.count("Svelte mount");
    return () => {
      app.$destroy();
    };
  }
};

export { entry as default };
